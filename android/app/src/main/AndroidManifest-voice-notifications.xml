<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add these permissions to the existing manifest for voice and notification features -->

    <!-- Continuous Voice Listening -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Notification Listener Service -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    
    <!-- Text-to-Speech -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <!-- Continuous Voice Listener Service -->
        <service
            android:name=".service.ContinuousVoiceListener"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Notification Interceptor Service -->
        <service
            android:name=".service.NotificationInterceptorService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Enhanced Main Activity -->
        <activity
            android:name=".ui.EnhancedMainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Myra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
