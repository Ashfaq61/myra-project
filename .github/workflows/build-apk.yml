name: Build MYRA APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Initialize and Build Advanced Android Structure
        run: |
          # Create directory structure
          mkdir -p app/src/main/java/com/myra/assistant
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/drawable
          mkdir -p app/src/main/res/mipmap-hdpi

          # Create local.properties
          echo "sdk.dir=$ANDROID_HOME" > local.properties

          # Create gradle.properties
          cat <<'EOF' > gradle.properties
          org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
          android.useAndroidX=true
          android.enableJetifier=true
          EOF

          # Create settings.gradle
          cat <<'EOF' > settings.gradle
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories {
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = "MyraAssistant"
          include ':app'
          EOF

          # Create root build.gradle
          cat <<'EOF' > build.gradle
          plugins {
              id 'com.android.application' version '8.2.0' apply false
          }
          EOF

          # Create app/build.gradle
          cat <<'EOF' > app/build.gradle
          plugins {
              id 'com.android.application'
          }

          android {
              namespace 'com.myra.assistant'
              compileSdk 34

              defaultConfig {
                  applicationId "com.myra.assistant"
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }

              buildTypes {
                  debug {
                      minifyEnabled false
                  }
                  release {
                      minifyEnabled false
                  }
              }

              compileOptions {
                  sourceCompatibility JavaVersion.VERSION_17
                  targetCompatibility JavaVersion.VERSION_17
              }
          }

          dependencies {
              implementation 'androidx.core:core:1.12.0'
          }
          EOF

          # Create strings.xml
          cat <<'EOF' > app/src/main/res/values/strings.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <string name="app_name">MYRA</string>
          </resources>
          EOF

          # Create colors.xml
          cat <<'EOF' > app/src/main/res/values/colors.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="colorPrimary">#00E5FF</color>
              <color name="colorBackground">#0A0B10</color>
          </resources>
          EOF

          # Create AndroidManifest.xml (FIXED xmlns:tools URL)
          cat <<'EOF' > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/apk/res-auto"
              package="com.myra.assistant">

              <uses-permission android:name="android.permission.RECORD_AUDIO" />
              <uses-permission android:name="android.permission.READ_PHONE_STATE" />
              <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
              <uses-permission android:name="android.permission.READ_CALL_LOG" />
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
              <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                  tools:ignore="ProtectedPermissions" />

              <application
                  android:allowBackup="true"
                  android:label="@string/app_name"
                  android:theme="@style/Theme.AppCompat.NoActionBar"
                  tools:ignore="MissingApplicationIcon">

                  <activity
                      android:name=".MainActivity"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>

                  <service
                      android:name=".MyraBackgroundService"
                      android:foregroundServiceType="microphone"
                      android:exported="false" />

                  <service
                      android:name=".MyraNotificationListener"
                      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.service.notification.NotificationListenerService" />
                      </intent-filter>
                  </service>
              </application>
          </manifest>
          EOF

          # Create glow_circle.xml
          cat <<'EOF' > app/src/main/res/drawable/glow_circle.xml
          <?xml version="1.0" encoding="utf-8"?>
          <shape xmlns:android="http://schemas.android.com/apk/res/android"
              android:shape="oval">
              <gradient
                  android:type="radial"
                  android:gradientRadius="150dp"
                  android:startColor="#BB00E5FF"
                  android:endColor="#00000000" />
              <stroke
                  android:width="3dp"
                  android:color="#00E5FF" />
              <size
                  android:width="200dp"
                  android:height="200dp" />
          </shape>
          EOF

          # Create mic_bg.xml
          cat <<'EOF' > app/src/main/res/drawable/mic_bg.xml
          <?xml version="1.0" encoding="utf-8"?>
          <shape xmlns:android="http://schemas.android.com/apk/res/android"
              android:shape="oval">
              <solid android:color="#1A1A22" />
              <stroke
                  android:width="2dp"
                  android:color="#00E5FF" />
              <size
                  android:width="90dp"
                  android:height="90dp" />
          </shape>
          EOF

          # Create activity_main.xml (FIXED syntax error)
          cat <<'EOF' > app/src/main/res/layout/activity_main.xml
          <?xml version="1.0" encoding="utf-8"?>
          <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#0A0B10"
              android:padding="20dp">

              <TextView
                  android:id="@+id/titleText"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:text="M Y R A"
                  android:textColor="#00E5FF"
                  android:textSize="32sp"
                  android:textStyle="bold"
                  android:letterSpacing="0.4"
                  android:layout_centerHorizontal="true"
                  android:layout_marginTop="30dp" />

              <FrameLayout
                  android:id="@+id/auraContainer"
                  android:layout_width="250dp"
                  android:layout_height="250dp"
                  android:layout_centerInParent="true">

                  <View
                      android:id="@+id/auraGlow"
                      android:layout_width="200dp"
                      android:layout_height="200dp"
                      android:layout_gravity="center"
                      android:background="@drawable/glow_circle" />
              </FrameLayout>

              <TextView
                  android:id="@+id/statusText"
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_below="@id/auraContainer"
                  android:layout_marginTop="30dp"
                  android:text="MYRA: Gemini Live Active hai! Bolen Ashfaq Bhai..."
                  android:textColor="#8A90A6"
                  android:textSize="18sp"
                  android:gravity="center" />

              <Button
                  android:id="@+id/micButton"
                  android:layout_width="95dp"
                  android:layout_height="95dp"
                  android:layout_alignParentBottom="true"
                  android:layout_centerHorizontal="true"
                  android:layout_marginBottom="50dp"
                  android:text="🎤"
                  android:textSize="36sp"
                  android:background="@drawable/mic_bg"
                  android:textColor="#00E5FF" />
          </RelativeLayout>
          EOF

          # Create MainActivity.java
          cat <<'EOF' > app/src/main/java/com/myra/assistant/MainActivity.java
          package com.myra.assistant;

          import android.app.Activity;
          import android.content.Intent;
          import android.os.Bundle;
          import android.view.animation.Animation;
          import android.view.animation.ScaleAnimation;
          import androidx.core.app.ActivityCompat;

          public class MainActivity extends Activity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);

                  android.view.View auraGlow = findViewById(R.id.auraGlow);
                  ScaleAnimation pulse = new ScaleAnimation(
                      1.0f, 1.2f, 1.0f, 1.2f,
                      Animation.RELATIVE_TO_SELF, 0.5f,
                      Animation.RELATIVE_TO_SELF, 0.5f
                  );
                  pulse.setDuration(700);
                  pulse.setRepeatMode(Animation.REVERSE);
                  pulse.setRepeatCount(Animation.INFINITE);
                  auraGlow.startAnimation(pulse);

                  ActivityCompat.requestPermissions(this, new String[]{
                      android.Manifest.permission.RECORD_AUDIO,
                      android.Manifest.permission.READ_PHONE_STATE,
                      android.Manifest.permission.ANSWER_PHONE_CALLS,
                      android.Manifest.permission.READ_CALL_LOG
                  }, 1);

                  try {
                      Intent intent = new Intent("android.settings.ACTION_NOTIFICATION_LISTENER_SETTINGS");
                      startActivity(intent);
                  } catch(Exception e) {}

                  Intent serviceIntent = new Intent(this, MyraBackgroundService.class);
                  startForegroundService(serviceIntent);
              }
          }
          EOF

          # Create MyraBackgroundService.java (FIXED: AsyncTask replaced with Thread)
          cat <<'EOF' > app/src/main/java/com/myra/assistant/MyraBackgroundService.java
          package com.myra.assistant;

          import android.app.Notification;
          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.app.Service;
          import android.content.Context;
          import android.content.Intent;
          import android.net.Uri;
          import android.os.Bundle;
          import android.os.Handler;
          import android.os.IBinder;
          import android.os.Looper;
          import android.telecom.TelecomManager;
          import android.speech.RecognitionListener;
          import android.speech.RecognizerIntent;
          import android.speech.SpeechRecognizer;
          import android.speech.tts.TextToSpeech;
          import java.io.BufferedReader;
          import java.io.InputStreamReader;
          import java.io.OutputStream;
          import java.net.HttpURLConnection;
          import java.net.URL;
          import java.util.ArrayList;
          import java.util.Locale;

          public class MyraBackgroundService extends Service {
              private SpeechRecognizer speechRecognizer;
              private TextToSpeech tts;
              private Intent speechIntent;
              private final Handler mainHandler = new Handler(Looper.getMainLooper());

              @Override
              public void onCreate() {
                  super.onCreate();

                  NotificationChannel channel = new NotificationChannel(
                      "myra_gemini",
                      "MYRA Gemini Live",
                      NotificationManager.IMPORTANCE_LOW
                  );
                  getSystemService(NotificationManager.class).createNotificationChannel(channel);

                  Notification notification = new Notification.Builder(this, "myra_gemini")
                      .setContentTitle("MYRA Gemini Engine Running")
                      .build();

                  startForeground(4, notification);

                  tts = new TextToSpeech(this, status -> {
                      if (status == TextToSpeech.SUCCESS) {
                          tts.setLanguage(new Locale("ur", "PK"));
                      }
                  });

                  startContinuousListening();
              }

              private void startContinuousListening() {
                  speechRecognizer = SpeechRecognizer.createSpeechRecognizer(this);
                  speechIntent = new Intent(RecognizerIntent.ACTION_RECOGNIZE_SPEECH);
                  speechIntent.putExtra(RecognizerIntent.EXTRA_LANGUAGE_MODEL,
                      RecognizerIntent.LANGUAGE_MODEL_FREE_FORM);
                  speechIntent.putExtra(RecognizerIntent.EXTRA_LANGUAGE, "ur-PK");

                  speechRecognizer.setRecognitionListener(new RecognitionListener() {
                      @Override
                      public void onResults(Bundle results) {
                          ArrayList<String> matches = results.getStringArrayList(
                              SpeechRecognizer.RESULTS_RECOGNITION);
                          if (matches != null && !matches.isEmpty()) {
                              String userWords = matches.get(0).toLowerCase();
                              handleSmartCommandsAndGemini(userWords);
                          }
                          speechRecognizer.startListening(speechIntent);
                      }

                      @Override
                      public void onError(int error) {
                          speechRecognizer.startListening(speechIntent);
                      }

                      @Override public void onReadyForSpeech(Bundle params) {}
                      @Override public void onBeginningOfSpeech() {}
                      @Override public void onRmsChanged(float rmsdB) {}
                      @Override public void onBufferReceived(byte[] buffer) {}
                      @Override public void onEndOfSpeech() {}
                      @Override public void onPartialResults(Bundle partialResults) {}
                      @Override public void onEvent(int eventType, Bundle params) {}
                  });

                  speechRecognizer.startListening(speechIntent);
              }

              private void handleSmartCommandsAndGemini(String words) {
                  if (words.contains("attend") || words.contains("call uthao") || words.contains("yes")) {
                      try {
                          TelecomManager telecomManager = (TelecomManager)
                              getSystemService(Context.TELECOM_SERVICE);
                          if (telecomManager != null && telecomManager.isRinging()) {
                              telecomManager.acceptRingingCall();
                              tts.speak("Call utha di hai Ashfaq Bhai.",
                                  TextToSpeech.QUEUE_FLUSH, null, null);
                              return;
                          }
                      } catch (Exception e) {}
                  }

                  if (words.contains("whatsapp")) {
                      try {
                          Intent whatsappIntent = new Intent(Intent.ACTION_VIEW);
                          whatsappIntent.setData(Uri.parse(
                              "https://api.whatsapp.com/send?text=" +
                              Uri.encode("MYRA Assistant Gemini Live mode par set ho chuki hai.")));
                          whatsappIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                          startActivity(whatsappIntent);
                          tts.speak("WhatsApp open kar diya hai.",
                              TextToSpeech.QUEUE_FLUSH, null, null);
                          return;
                      } catch (Exception e) {}
                  }

                  if (words.contains("myra") || words.contains("maira") || words.length() > 3) {
                      new Thread(() -> {
                          String result = callGeminiAPI(words);
                          mainHandler.post(() -> {
                              if (result != null && tts != null) {
                                  tts.speak(result, TextToSpeech.QUEUE_FLUSH, null, null);
                              }
                          });
                      }).start();
                  }
              }

              private String callGeminiAPI(String userPrompt) {
                  try {
                      String apiKey = System.getenv("GEMINI_API_KEY");
                      if (apiKey == null || apiKey.isEmpty()) {
                          return "API Key missing hai. Settings check karein.";
                      }

                      URL url = new URL("https://generativelanguage.googleapis.com/v1beta/models/" +
                          "gemini-1.5-flash:generateContent?key=" + apiKey);

                      HttpURLConnection conn = (HttpURLConnection) url.openConnection();
                      conn.setRequestMethod("POST");
                      conn.setRequestProperty("Content-Type", "application/json");
                      conn.setDoOutput(true);
                      conn.setConnectTimeout(10000);
                      conn.setReadTimeout(10000);

                      String systemInstruction = "You are MYRA, a super-smart personal AI " +
                          "assistant created by Ashfaq Bhai. Talk politely in mixed Urdu and English. " +
                          "Keep responses friendly, witty, and concise.";

                      String jsonInput = "{\"contents\": [{\"parts\":[{\"text\":\"" +
                          systemInstruction + " User says: " + userPrompt.replace("\"", "\\\"") +
                          "\"}]}]}";

                      try (OutputStream os = conn.getOutputStream()) {
                          byte[] input = jsonInput.getBytes("utf-8");
                          os.write(input, 0, input.length);
                      }

                      BufferedReader br = new BufferedReader(
                          new InputStreamReader(conn.getInputStream(), "utf-8"));
                      StringBuilder response = new StringBuilder();
                      String responseLine;
                      while ((responseLine = br.readLine()) != null) {
                          response.append(responseLine.trim());
                      }

                      String rawResponse = response.toString();
                      if (rawResponse.contains("\"text\": \"")) {
                          int start = rawResponse.indexOf("\"text\": \"") + 9;
                          int end = rawResponse.indexOf("\"", start);
                          if (end > start) {
                              String aiReply = rawResponse.substring(start, end);
                              return aiReply.replace("\\n", " ").replace("\\\"", "\"");
                          }
                      }
                      return "Kuch samajh nahi aaya, phir se bolain.";
                  } catch (Exception e) {
                      return "Network connection check karein Ashfaq Bhai.";
                  }
              }

              @Override
              public IBinder onBind(Intent intent) {
                  return null;
              }

              @Override
              public void onDestroy() {
                  super.onDestroy();
                  if (speechRecognizer != null) {
                      speechRecognizer.destroy();
                  }
                  if (tts != null) {
                      tts.stop();
                      tts.shutdown();
                  }
              }
          }
          EOF

          # Create MyraNotificationListener.java
          cat <<'EOF' > app/src/main/java/com/myra/assistant/MyraNotificationListener.java
          package com.myra.assistant;

          import android.service.notification.NotificationListenerService;
          import android.service.notification.StatusBarNotification;
          import android.speech.tts.TextToSpeech;
          import java.util.Locale;

          public class MyraNotificationListener extends NotificationListenerService {
              private TextToSpeech tts;

              @Override
              public void onCreate() {
                  super.onCreate();
                  tts = new TextToSpeech(this, status -> {
                      if (status == TextToSpeech.SUCCESS) {
                          tts.setLanguage(new Locale("ur", "PK"));
                      }
                  });
              }

              @Override
              public void onNotificationPosted(StatusBarNotification sbn) {
                  String pack = sbn.getPackageName();
                  android.app.Bundle extras = sbn.getNotification().extras;
                  String title = extras.getString("android.title");

                  if (title != null && !pack.contains("com.myra.assistant")) {
                      String msg = "Ashfaq Bhai, naya notification aaya hai.";
                      if (pack.contains("whatsapp")) {
                          msg = "Ashfaq Bhai, WhatsApp par " + title + " ka message aaya hai.";
                      } else if (pack.contains("telecom") || pack.contains("dialer")) {
                          msg = "Ashfaq Bhai, " + title + " ki call aa rahi hai.";
                      }
                      tts.speak(msg, TextToSpeech.QUEUE_ADD, null, null);
                  }
              }

              @Override
              public void onDestroy() {
                  super.onDestroy();
                  if (tts != null) {
                      tts.stop();
                      tts.shutdown();
                  }
              }
          }
          EOF

      - name: Setup Gradle Wrapper
        run: |
          mkdir -p gradle/wrapper
          cat <<'EOF' > gradle/wrapper/gradle-wrapper.properties
          distributionBase=GRADLE_USER_HOME
          distributionPath=wrapper/dists
          distributionUrl=https\://services.gradle.org/distributions/gradle-8.2-bin.zip
          networkTimeout=10000
          validateDistributionUrl=true
          zipStoreBase=GRADLE_USER_HOME
          zipStorePath=wrapper/dists
          EOF

      - name: Download Gradle Wrapper
        run: |
          curl -L -o gradle/wrapper/gradle-wrapper.jar \
            https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar

      - name: Make Gradle Wrapper Executable
        run: chmod +x gradlew

      - name: Build APK with Gradle
        run: ./gradlew :app:assembleDebug --no-daemon --stacktrace
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Rename APK
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk myra-assistant.apk

      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: myra-assistant-apk
          path: myra-assistant.apk
          retention-days: 30

      - name: Create GitHub Release with APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "MYRA Assistant Build v${{ github.run_number }}"
          body: "MYRA Assistant APK - Auto Build"
          files: myra-assistant.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
